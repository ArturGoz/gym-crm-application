<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="
      http://www.liquibase.org/xml/ns/dbchangelog
      http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.18.xsd">

    <changeSet id="GCA-35-update-data" author="ArturGoz">
        <sql>
            UPDATE users
            SET password = 'hashed_password_trainee32'
            WHERE password = 'hashed_password_trainee';
        </sql>
    </changeSet>

    <changeSet id="GCA-29-change-setval" author="ArturGoz">
        <sql>
            SELECT setval('users_id_seq', (SELECT MAX(id) FROM users), true);
            SELECT setval('training_types_id_seq', (SELECT MAX(id) FROM training_types), true);
            SELECT setval('trainers_id_seq', (SELECT MAX(id) FROM trainers), true);
            SELECT setval('trainees_id_seq', (SELECT MAX(id) FROM trainees), true);
            SELECT setval('trainings_id_seq', (SELECT MAX(id) FROM trainings), true);

        </sql>
    </changeSet>

</databaseChangeLog>
